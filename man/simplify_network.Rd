% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_graph.R
\name{simplify_network}
\alias{simplify_network}
\title{Simplify a river network graph}
\usage{
simplify_network(graph, gauge_nodes = character(0))
}
\arguments{
\item{graph}{A directed \code{igraph} as returned by \code{\link[=build_ngd_graph]{build_ngd_graph()}}.}

\item{gauge_nodes}{Named character vector mapping gauge display labels to
node IDs in \code{graph}. As returned by \code{\link[=gauge_node_map]{gauge_node_map()}}. Use
\code{character(0)} or \code{c()} if there are no gauges.}
}
\value{
A list with two elements:
\describe{
\item{\code{graph}}{Simplified directed \code{igraph}}
\item{\code{node_meta}}{Data frame with columns \code{name}, \code{type}, \code{label}
for use in layout and plotting}
}
}
\description{
Contracts intermediate (degree-2) nodes, retaining only:
\itemize{
\item \strong{Confluences} — nodes with in-degree > 1 (more than one upstream reach)
\item \strong{Gauges} — nodes corresponding to snapped gauge positions
\item \strong{Headwaters} — nodes with in-degree 0
\item \strong{Outlet} — node(s) with out-degree 0
}
}
\details{
Each edge in the simplified graph carries an \code{n_reaches} attribute
recording how many original reaches were collapsed into it.
}
\examples{
\dontrun{
g        <- build_ngd_graph(network)
g_map    <- gauge_node_map(gauges_snapped, network)
result   <- simplify_network(g, g_map)
g_simple <- result$graph
}

}
