% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_ngd.R
\name{load_ngd_network}
\alias{load_ngd_network}
\title{Load OS NGD Water Network}
\usage{
load_ngd_network(path, layer = "wtr_ntwk_waterlink", crs = 27700, ...)
}
\arguments{
\item{path}{Character. Path to the OS NGD GeoPackage (\code{.gpkg}).}

\item{layer}{Character. Layer name to read. Defaults to
\code{"wtr_ntwk_waterlink"}.}

\item{crs}{Integer. Target EPSG code. Defaults to \code{27700} (BNG).}

\item{...}{Additional arguments passed to \code{\link[sf:st_read]{sf::st_read()}}, e.g.
\code{query = "SELECT * FROM wtr_ntwk_waterlink WHERE permanence = 'Permanent'"}.}
}
\value{
An sf object with LINESTRING geometry and at minimum the columns
\code{osid}, \code{startnode}, \code{endnode}. The column \code{geometry_length_m} is retained
if present in the source layer.
}
\description{
Reads the \code{wtr_ntwk_waterlink} layer from an OS NGD GeoPackage and
returns a clean sf LINESTRING object in EPSG:27700 (British National Grid).
}
\details{
The OS NGD Water Link layer may be stored in 3D (EPSG:7405, BNG + ODN
height). Z coordinates are dropped automatically so that subsequent spatial
operations work correctly in 2D.

\strong{Required source columns}\tabular{ll}{
   Column \tab Description \cr
   \code{osid} \tab Unique OS identifier (GUID) for each reach \cr
   \code{startnode} \tab OS node ID at the upstream end of the reach \cr
   \code{endnode} \tab OS node ID at the downstream end of the reach \cr
}


If any of these columns are absent the function stops with an informative
error so you can verify the layer name or data version.
}
\examples{
\dontrun{
network <- load_ngd_network("path/to/os_ngd_water.gpkg")
}

}
