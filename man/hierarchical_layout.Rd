% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layout.R
\name{hierarchical_layout}
\alias{hierarchical_layout}
\title{Compute a hierarchical layout for a simplified river network}
\usage{
hierarchical_layout(g_simple, node_meta, min_gap = 0.14)
}
\arguments{
\item{g_simple}{Simplified directed \code{igraph} as returned by the \verb{$graph}
element of \code{\link[=simplify_network]{simplify_network()}}.}

\item{node_meta}{Data frame with columns \code{name}, \code{type}, \code{label} as
returned by the \verb{$node_meta} element of \code{\link[=simplify_network]{simplify_network()}}.}

\item{min_gap}{Numeric. Minimum centre-to-centre horizontal distance
between nodes on the same depth level. Tune upward if boxes overlap on
wide, bushy networks. Default \code{0.14}.}
}
\value{
A data frame with one row per node and columns:
\describe{
\item{\code{name}}{Node ID (matches \code{igraph} vertex names)}
\item{\code{x}}{Horizontal position in [0.05, 0.95]}
\item{\code{y}}{Vertical position (negative depth, so outlet is at 0 and
headwaters are most negative)}
\item{\code{type}}{Node type from \code{node_meta}}
\item{\code{label}}{Display label from \code{node_meta}}
}
}
\description{
Places nodes top-to-bottom by topological depth (longest path from any
headwater) and distributes them horizontally in proportion to their
subtree widths. A post-layout spacing pass enforces a minimum gap between
nodes on the same row, and the result is rescaled to [0.05, 0.95].
}
\details{
This is the same algorithm used in the large synthetic example, extracted
into a reusable function.
}
