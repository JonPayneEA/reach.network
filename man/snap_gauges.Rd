% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snap_gauges.R
\name{snap_gauges}
\alias{snap_gauges}
\title{Snap gauges to the nearest river reach}
\usage{
snap_gauges(gauges, network, tolerance_m = 50, snap_types = c("flow", "stage"))
}
\arguments{
\item{gauges}{An sf object of gauge points. Must contain:
\itemize{
\item \code{gauge_id}   — unique identifier
\item \code{gauge_name} — display name
\item \code{gauge_type} — type string, e.g. \code{"flow"}, \code{"stage"}, \code{"rain"}
}}

\item{network}{An sf LINESTRING object as returned by \code{\link[=load_ngd_network]{load_ngd_network()}}.
Must contain the \code{osid} column.}

\item{tolerance_m}{Numeric. Maximum snapping distance in metres. Gauges
further than this from any reach are flagged as unsnapped. Default \code{50}.}

\item{snap_types}{Character vector. Gauge types that should be snapped to
the network. Default \code{c("flow", "stage")}. All other types are left
unsnapped.}
}
\value{
The input \code{gauges} sf object with three additional columns:
\describe{
\item{\code{snapped_osid}}{\code{osid} of the nearest reach (NA if unsnapped)}
\item{\code{snap_distance_m}}{Distance in metres to the nearest reach
(NA for types not in \code{snap_types})}
\item{\code{snapped}}{Logical: TRUE if snapped successfully}
}
Gauge geometries are \strong{not moved} — the original point locations are
preserved for the spatial plot.
}
\description{
For each gauge whose \code{gauge_type} is in \code{snap_types}, finds the nearest
watercourse link in \code{network} and records its \code{osid} as the anchor for
graph construction. Gauges outside \code{tolerance_m} are flagged as unsnapped
but retained.
}
\details{
\strong{Rain gauges are not snapped.} They sit anywhere in the catchment and
snapping them to the nearest river line is meaningless. They appear in the
spatial plot at their true location and in the schematic only via model
metadata (not via river topology). Pass \code{gauge_type = "rain"} (or whichever
string your data uses) and ensure it is absent from \code{snap_types}.

The function also prints a summary table of snapping results and issues a
warning for any gauge that is eligible for snapping but falls outside the
tolerance.
}
\examples{
\dontrun{
network <- load_ngd_network("os_ngd_water.gpkg")
gauges  <- sf::st_read("gauges.gpkg")
gauges_snapped <- snap_gauges(gauges, network, tolerance_m = 100)
}

}
