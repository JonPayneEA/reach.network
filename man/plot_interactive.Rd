% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_interactive.R
\name{plot_interactive}
\alias{plot_interactive}
\title{Interactive network diagram (HTML widget)}
\usage{
plot_interactive(
  g_simple,
  layout_df,
  model_meta = NULL,
  rain_gauges = NULL,
  height = "700px"
)
}
\arguments{
\item{g_simple}{Simplified directed \code{igraph} (\verb{$graph} from
\code{\link[=simplify_network]{simplify_network()}}).}

\item{layout_df}{Layout data frame as returned by \code{\link[=hierarchical_layout]{hierarchical_layout()}}.}

\item{model_meta}{Optional data frame with columns \code{gauge_id} and
\code{model_name}. Model names are appended to gauge tooltips and labels.
Default \code{NULL}.}

\item{rain_gauges}{Optional data frame/sf of rain gauges (rows with
\code{gauge_type == "rain"}). When supplied, rain gauges are added as
disconnected nodes in the widget. Default \code{NULL}.}

\item{height}{Character. Widget height passed to \code{visNetwork()}.
Default \code{"700px"}.}
}
\value{
A \code{visNetwork} HTML widget. Render it in RStudio Viewer, embed it
in a Quarto/R Markdown document, or save it with
\code{visNetwork::visSave(widget, "output.html")}.
}
\description{
Builds a \code{visNetwork} HTML widget from the simplified river network.
The widget supports zooming, panning, hover tooltips, and
click-to-highlight nearest neighbours.
}
\details{
Node colours and shapes follow the same palette as the static plots:
\itemize{
\item \strong{Gauge} (flow/stage): orange rectangle
\item \strong{Rain gauge}: blue diamond
\item \strong{Confluence}: grey rectangle
\item \strong{Headwater}: green rectangle
\item \strong{Outlet}: teal rectangle (double border)
}
}
\examples{
\dontrun{
result  <- simplify_network(g, gauge_node_map(gauges_snapped, network))
layout  <- hierarchical_layout(result$graph, result$node_meta)
widget  <- plot_interactive(result$graph, layout)
visNetwork::visSave(widget, "river_network.html")
}

}
