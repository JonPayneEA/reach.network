% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_spatial.R
\name{plot_spatial}
\alias{plot_spatial}
\title{Spatial map of the river network and gauge locations}
\usage{
plot_spatial(network, gauges, model_meta = NULL, label_offset = 200)
}
\arguments{
\item{network}{An sf LINESTRING object as returned by \code{\link[=load_ngd_network]{load_ngd_network()}}.}

\item{gauges}{An sf POINT object as returned by \code{\link[=snap_gauges]{snap_gauges()}}. Must have
columns \code{gauge_id}, \code{gauge_name}, \code{gauge_type}, \code{snapped}.}

\item{model_meta}{Optional data frame with columns \code{gauge_id} and
\code{model_name} (long format, one row per gauge-model pair). When supplied,
model names are appended to gauge labels. Default \code{NULL}.}

\item{label_offset}{Numeric. Horizontal nudge for gauge labels in map units
(metres for BNG). Default \code{200}.}
}
\value{
A ggplot2 object.
}
\description{
Renders the raw river network lines with gauge points overlaid. Snapped
flow/stage gauges are shown as filled orange circles; unsnapped gauges and
rain gauges are shown with distinct styling so their status is immediately
clear.
}
\examples{
\dontrun{
p <- plot_spatial(network, gauges_snapped)
print(p)
}

}
