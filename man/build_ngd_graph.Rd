% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_graph.R
\name{build_ngd_graph}
\alias{build_ngd_graph}
\title{Build a directed graph from OS NGD Water Network}
\usage{
build_ngd_graph(network)
}
\arguments{
\item{network}{An sf LINESTRING object as returned by \code{\link[=load_ngd_network]{load_ngd_network()}}.
Must contain \code{osid}, \code{startnode}, and \code{endnode}.}
}
\value{
A directed \code{igraph} object. Each edge represents one watercourse
link; edge attributes include \code{reach_id} (= \code{osid}) and \code{length_m}
(if \code{geometry_length_m} is present in \code{network}).
}
\description{
Constructs an \code{igraph} directed graph using the \code{startnode}/\code{endnode}
columns from the OS NGD Water Link layer. This is more reliable than
reconstructing topology from endpoint coordinates (which requires
floating-point matching and fails on large networks with tiny gaps).
}
\examples{
\dontrun{
network <- load_ngd_network("os_ngd_water.gpkg")
g <- build_ngd_graph(network)
}

}
