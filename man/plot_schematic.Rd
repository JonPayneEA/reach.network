% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_schematic.R
\name{plot_schematic}
\alias{plot_schematic}
\title{Schematic diagram of a simplified river network}
\usage{
plot_schematic(g_simple, layout_df, model_meta = NULL, rain_gauges = NULL)
}
\arguments{
\item{g_simple}{Simplified directed \code{igraph} (\verb{$graph} from
\code{\link[=simplify_network]{simplify_network()}}).}

\item{layout_df}{Layout data frame as returned by \code{\link[=hierarchical_layout]{hierarchical_layout()}}.}

\item{model_meta}{Optional data frame with columns \code{gauge_id} and
\code{model_name}. Default \code{NULL}.}

\item{rain_gauges}{Optional sf or data frame of rain gauges (rows with
\code{gauge_type == "rain"}). When supplied, rain gauges are plotted as
a separate panel column. Default \code{NULL}.}
}
\value{
A ggplot2 object.
}
\description{
Renders a top-to-bottom flow diagram with L-bend edges and colour-coded
node boxes. Each edge carries a badge showing how many original reaches
were collapsed into it.
}
\details{
If \code{model_meta} is supplied, gauge labels gain a third line listing the
models served by that gauge (e.g. \code{"G001\\nMain Gauge\\nHEC-RAS \\u00b7 ISIS"}).
Rain gauges (those not anchored to the river network) are shown in a
separate column to the right with a distinct colour, connected to their
model annotations via dashed lines.
}
\examples{
\dontrun{
result   <- simplify_network(g, gauge_node_map(gauges_snapped, network))
layout   <- hierarchical_layout(result$graph, result$node_meta)
p_schema <- plot_schematic(result$graph, layout)
}

}
