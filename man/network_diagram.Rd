% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_diagram.R
\name{network_diagram}
\alias{network_diagram}
\title{Build and save river network diagrams}
\usage{
network_diagram(
  network_path,
  gauge_path,
  model_meta_path = NULL,
  output_dir = ".",
  tolerance_m = 50,
  snap_types = c("flow", "stage"),
  network_layer = "wtr_ntwk_waterlink",
  gauge_type_col = "gauge_type",
  label_col = "gauge_name",
  save_png = TRUE,
  save_html = TRUE,
  ...
)
}
\arguments{
\item{network_path}{Character. Path to the OS NGD GeoPackage.}

\item{gauge_path}{Character. Path to the gauge GeoPackage or Shapefile.}

\item{model_meta_path}{Character or \code{NULL}. Path to a long-format CSV with
columns \code{gauge_id} and \code{model_name}. See \code{\link[=model_meta_template]{model_meta_template()}} for the
expected structure. Default \code{NULL} (no model annotations).}

\item{output_dir}{Character. Directory to write output files.
Created if it does not exist. Default \code{"."}.}

\item{tolerance_m}{Numeric. Snapping tolerance in metres. Default \code{50}.}

\item{snap_types}{Character vector. Gauge types to snap. Default
\code{c("flow", "stage")}.}

\item{network_layer}{Character. GeoPackage layer name for the network.
Default \code{"wtr_ntwk_waterlink"}.}

\item{gauge_type_col}{Character. Column in the gauge file that holds the
gauge type string. Default \code{"gauge_type"}.}

\item{label_col}{Character. Column in the gauge file used for node labels.
Default \code{"gauge_name"}.}

\item{save_png}{Logical. Save static PNG outputs. Default \code{TRUE}.}

\item{save_html}{Logical. Save interactive HTML widget. Default \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[=load_ngd_network]{load_ngd_network()}}.}
}
\value{
Invisibly, a named list:
\describe{
\item{\code{network}}{Loaded sf network}
\item{\code{gauges}}{Snapped gauges sf}
\item{\code{model_meta}}{Model metadata data frame (or \code{NULL})}
\item{\code{g_full}}{Full directed igraph}
\item{\code{g_simple}}{Simplified igraph}
\item{\code{node_meta}}{Node metadata data frame}
\item{\code{layout_df}}{Layout data frame}
\item{\code{p_spatial}}{ggplot2 spatial plot}
\item{\code{p_schematic}}{ggplot2 schematic plot}
\item{\code{p_combined}}{patchwork combined plot}
\item{\code{widget}}{visNetwork widget}
}
}
\description{
Top-level wrapper that runs the full pipeline:
}
\details{
\enumerate{
\item Load OS NGD Water Network from GeoPackage
\item Load gauge points from GeoPackage/Shapefile
\item (Optionally) load model metadata from CSV
\item Snap flow/stage gauges to the network
\item Build directed graph using \code{startnode}/\code{endnode}
\item Simplify the graph (keep confluences, gauges, headwaters, outlet)
\item Compute hierarchical layout
\item Produce and save: spatial PNG, schematic PNG, combined PNG, and
interactive HTML
}

Returns all intermediate objects invisibly so you can inspect or replot
any stage without re-running the full pipeline.
}
\examples{
\dontrun{
# Minimal call — no model metadata
result <- network_diagram(
  network_path = "os_ngd_water.gpkg",
  gauge_path   = "gauges.gpkg",
  output_dir   = "outputs"
)

# With model metadata
result <- network_diagram(
  network_path    = "os_ngd_water.gpkg",
  gauge_path      = "gauges.gpkg",
  model_meta_path = "model_metadata.csv",
  output_dir      = "outputs",
  tolerance_m     = 100
)

# Re-plot the schematic without re-running the pipeline
print(result$p_schematic)
}

}
